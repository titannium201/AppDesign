/**
 * TI 腿部恢复智能系统 — Mobile App 入口
 *
 * 当前为骨架阶段，仅导出应用根组件与模块导航类型。
 * 后续按 P0 模块（M1-M7）逐步填充页面与业务逻辑。
 */

export { default as App } from './App';
export type { MobileAppProps, RootStackParamList } from './types';
